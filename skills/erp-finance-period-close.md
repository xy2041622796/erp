# 财务期间结账与反结账页面

- 反结账视图规则：只展示 `close_status = 1` 的已结账期间；`carry_forward_status = 1 && close_status = 0` 表示已反结账后保留结转损益凭证，不再出现在反结账列表中。
- 月份卡片状态样式：`unprocessed` 未结账卡片使用更深蓝色背景和边框，明显区别于 `ready-close` 已结转损益未结账的浅蓝状态；`closed` 已结账保持灰色锁定样式。
- 月份卡片展示规则：卡片只展示月份、状态和状态图标，不再展示底部公司/账套名称，避免卡片内容拥挤。
