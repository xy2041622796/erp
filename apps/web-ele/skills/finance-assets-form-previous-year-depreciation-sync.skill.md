# 固定资产表单：以前年度累计折旧同步累计折旧

- 页面入口：财务系统 / 固定资产 / 资产管理 / 新增资产或编辑资产弹窗。
- 页面文件：`src/views/finance/assets/manage/modules/form.vue`。
- 核心能力：资产数据区输入或变更“累计折旧”后，“以前年度累计折旧”自动同步为相同金额。
- 关联计算：同步发生在 `updateComputedAmounts` 中，同时更新预计残值、剩余使用月份、净值、月折旧/摊销额。
- 当前规则：`以前年度累计折旧 = 累计折旧`；本年累计折旧保持独立字段，不参与该同步。