# Finance Settings Auxiliary 宽度适配 skill

- 入口文件：`src/views/erp/finance/settings/auxiliary/modules/aux-type-form.vue`
- 页面能力：辅助核算类型表单维护。
- 本次改动：将表单弹窗宽度从固定 `640px` 调整为 `min(40rem, 92vw)`。
- 使用到的数据或接口：辅助核算类型维护表单。
- 编排注意事项：该表单宽度相对较窄，保留轻量弹窗观感，同时去除固定像素依赖。
