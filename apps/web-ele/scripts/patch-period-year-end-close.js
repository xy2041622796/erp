const fs = require('fs');
const path = require('path');

function replaceExact(source, before, after, label) {
  if (!source.includes(before)) {
    throw new Error(`未找到待替换片段: ${label}`);
  }
  return source.replace(before, after);
}

const root = process.cwd();
const apiFile = path.join(root, 'lmbill/apps/web-ele/src/api/erp/finance/period/index.ts');
const viewFile = path.join(root, 'lmbill/apps/web-ele/src/views/erp/finance/period/index.vue');
const skillFile = path.join(root, 'lmbill/apps/web-ele/src/views/erp/finance/period/skill.md');

let api = fs.readFileSync(apiFile, 'utf8');

api = replaceExact(
  api,
  "const PROFIT_LOSS_SUBJECT_TYPE = '5';\nconst CURRENT_YEAR_PROFIT_CODE = '3103';\nconst RETAINED_EARNINGS_CODE = '3104006';\nconst EPSILON = 0.004;\n",
  "const PROFIT_LOSS_SUBJECT_TYPE = '5';\nconst CURRENT_YEAR_PROFIT_CODE = '3103';\nconst RETAINED_EARNINGS_CODE = '3104006';\nconst INCOME_TAX_EXPENSE_CODE = '5801';\nconst EPSILON = 0.004;\n",
  'constants',
);

api = replaceExact(
  api,
  `export interface PeriodClosePreviewResult {\n  period: string;\n  startDate: string;\n  endDate: string;\n  voucherDate: string;\n  incomeLines: PeriodCloseLine[];\n  expenseLines: PeriodCloseLine[];\n  totalIncome: number;\n  totalExpense: number;\n  profitAmount: number;\n  warnings: PeriodCloseWarningItem[];\n  existingVoucher: null | {\n    rowid?: string;\n    voucher_code?: string;\n    business_code?: string;\n    business_name?: string;\n    voucher_date?: string;\n  };\n  existingVoucherReusable: boolean;\n  existingVoucherReuseMessage: string;\n  currentYearProfitSubject: {\n    code: string;\n    name: string;\n  };\n  retainedEarningsSubject: null | {\n    code: string;\n    name: string;\n  };\n}\n`,
  `export interface PeriodClosePreviewResult {\n  period: string;\n  startDate: string;\n  endDate: string;\n  voucherDate: string;\n  incomeLines: PeriodCloseLine[];\n  expenseLines: PeriodCloseLine[];\n  totalIncome: number;\n  totalExpense: number;\n  profitAmount: number;\n  warnings: PeriodCloseWarningItem[];\n  existingVoucher: null | {\n    rowid?: string;\n    voucher_code?: string;\n    business_code?: string;\n    business_name?: string;\n    voucher_date?: string;\n  };\n  existingVoucherReusable: boolean;\n  existingVoucherReuseMessage: string;\n  currentYearProfitSubject: {\n    code: string;\n    name: string;\n  };\n  retainedEarningsSubject: null | {\n    code: string;\n    name: string;\n  };\n  incomeTaxExpenseSubject: null | {\n    code: string;\n    name: string;\n  };\n  yearEndTransferRequired: boolean;\n  yearEndCurrentYearProfitBalance: number;\n  yearEndTransferAmount: number;\n}\n`,
  'PeriodClosePreviewResult',
);

api = replaceExact(
  api,
  `function buildSubjectBuckets(subjects: any[]) {\n  const incomeMap = new Map<string, string>();\n  const expenseMap = new Map<string, string>();\n  let currentYearProfitSubject: null | { code: string; name: string } = null;\n  let retainedEarningsSubject: null | { code: string; name: string } = null;\n\n  for (const item of subjects) {\n    const code = String(item?.subject_number || '').trim();\n    if (!code) continue;\n    const name = String(item?.subject_name || code).trim() || code;\n    const direction = normalizeDirection(item?.balance_direction);\n    const subjectType = String(item?.subject_type || '').trim();\n\n    if (subjectType === PROFIT_LOSS_SUBJECT_TYPE) {\n      if (direction === 2) incomeMap.set(code, name);\n      if (direction === 1) expenseMap.set(code, name);\n    }\n    if (code === CURRENT_YEAR_PROFIT_CODE) {\n      currentYearProfitSubject = { code, name: name || '本年利润' };\n    }\n    if (code === RETAINED_EARNINGS_CODE) {\n      retainedEarningsSubject = { code, name: name || '未分配利润' };\n    }\n  }\n\n  if (!currentYearProfitSubject) {\n    throw new Error('未找到科目 3103（本年利润），无法执行期间结转');\n  }\n\n  return {\n    incomeMap,\n    expenseMap,\n    currentYearProfitSubject,\n    retainedEarningsSubject,\n  };\n}\n`,
  `function buildSubjectBuckets(subjects: any[]) {\n  const incomeMap = new Map<string, string>();\n  const expenseMap = new Map<string, string>();\n  let currentYearProfitSubject: null | { code: string; name: string } = null;\n  let retainedEarningsSubject: null | { code: string; name: string } = null;\n  let incomeTaxExpenseSubject: null | { code: string; name: string } = null;\n\n  for (const item of subjects) {\n    const code = String(item?.subject_number || '').trim();\n    if (!code) continue;\n    const name = String(item?.subject_name || code).trim() || code;\n    const direction = normalizeDirection(item?.balance_direction);\n    const subjectType = String(item?.subject_type || '').trim();\n\n    if (subjectType === PROFIT_LOSS_SUBJECT_TYPE) {\n      if (direction === 2) incomeMap.set(code, name);\n      if (direction === 1) expenseMap.set(code, name);\n    }\n\n    if (direction === 1 && (code === INCOME_TAX_EXPENSE_CODE || /所得税/.test(name))) {\n      expenseMap.set(code, name);\n      if (!incomeTaxExpenseSubject || code === INCOME_TAX_EXPENSE_CODE) {\n        incomeTaxExpenseSubject = { code, name: name || '所得税费用' };\n      }\n    }\n\n    if (code === CURRENT_YEAR_PROFIT_CODE) {\n      currentYearProfitSubject = { code, name: name || '本年利润' };\n    }\n    if (code === RETAINED_EARNINGS_CODE) {\n      retainedEarningsSubject = { code, name: name || '未分配利润' };\n    }\n  }\n\n  if (!currentYearProfitSubject) {\n    throw new Error('未找到科目 3103（本年利润），无法执行期间结转');\n  }\n\n  return {\n    incomeMap,\n    expenseMap,\n    currentYearProfitSubject,\n    retainedEarningsSubject,\n    incomeTaxExpenseSubject,\n  };\n}\n`,
  'buildSubjectBuckets',
);

api = replaceExact(
  api,
  `function buildReverseRemark(period: string, carryForwardVoucherCode?: string) {\n  return carryForwardVoucherCode\n    ? \`期间 \${period} 已反结账，结转损益凭证保留：\${carryForwardVoucherCode}\`\n    : \`期间 \${period} 已反结账，无需保留结转损益凭证\`;\n}\n\nfunction buildExpectedPeriodCloseEntries(preview: PeriodClosePreviewResult) {\n`,
  `function buildReverseRemark(period: string, carryForwardVoucherCode?: string) {\n  return carryForwardVoucherCode\n    ? \`期间 \${period} 已反结账，结转损益凭证保留：\${carryForwardVoucherCode}\`\n    : \`期间 \${period} 已反结账，无需保留结转损益凭证\`;\n}\n\nfunction isYearEndPeriod(period: string) {\n  return String(period || '').trim().endsWith('-12');\n}\n\nfunction buildCurrentYearProfitEntryFromPreview(preview: PeriodClosePreviewResult) {\n  let currentYearProfitDebit = 0;\n  let currentYearProfitCredit = 0;\n\n  for (const row of preview.incomeLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    if (row.amount > 0) {\n      currentYearProfitCredit = round2(currentYearProfitCredit + amount);\n    } else {\n      currentYearProfitDebit = round2(currentYearProfitDebit + amount);\n    }\n  }\n\n  for (const row of preview.expenseLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    if (row.amount > 0) {\n      currentYearProfitDebit = round2(currentYearProfitDebit + amount);\n    } else {\n      currentYearProfitCredit = round2(currentYearProfitCredit + amount);\n    }\n  }\n\n  const currentYearProfitNet = round2(currentYearProfitDebit - currentYearProfitCredit);\n  return {\n    debit: currentYearProfitNet > 0 ? currentYearProfitNet : 0,\n    credit: currentYearProfitNet < 0 ? Math.abs(currentYearProfitNet) : 0,\n    balanceChange: round2((currentYearProfitNet < 0 ? Math.abs(currentYearProfitNet) : 0) - (currentYearProfitNet > 0 ? currentYearProfitNet : 0)),\n  };\n}\n\nasync function getCurrentYearProfitBalanceForYearEnd(options: {\n  period: string;\n  endDate: string;\n  accountSetId?: string;\n  companyName?: string;\n  excludeVoucherId?: string;\n}) {\n  const [yearText] = String(options.period || '').split('-');\n  const fiscalYear = Number(yearText);\n  if (!fiscalYear) return 0;\n\n  const startDate = \`${yearText}-01-01\`;\n  const mains = await queryVoucherMains({\n    startDate,\n    endDate: options.endDate,\n    accountSetId: options.accountSetId,\n    companyName: options.companyName,\n  });\n\n  const voucherIds = (mains || [])\n    .map((item: any) => String(item?.rowid || '').trim())\n    .filter(Boolean);\n  const details = await queryVoucherDetailsByVoucherIds(voucherIds, options.accountSetId);\n  const detailMap = buildDetailMap(details as any[]);\n  const excludeVoucherId = String(options.excludeVoucherId || '').trim();\n\n  let balance = 0;\n  for (const main of mains as any[]) {\n    const voucherId = String(main?.rowid || '').trim();\n    if (!voucherId) continue;\n    if (voucherId === excludeVoucherId) continue;\n    if (Number(main?.is_reversed ?? 0) === 1) continue;\n\n    const detailsOfVoucher = detailMap.get(voucherId) || [];\n    for (const detail of detailsOfVoucher as any[]) {\n      const accountCode = String(detail?.account_code || '').trim();\n      if (accountCode !== CURRENT_YEAR_PROFIT_CODE) continue;\n      const debit = Number(detail?.debit_amount || 0);\n      const credit = Number(detail?.credit_amount || 0);\n      balance = round2(balance + credit - debit);\n    }\n  }\n\n  return balance;\n}\n\nfunction buildExpectedPeriodCloseEntries(preview: PeriodClosePreviewResult) {\n`,
  'insert year-end helpers',
);

api = api.replace(/function buildExpectedPeriodCloseEntries\(preview: PeriodClosePreviewResult\) \{[\s\S]*?\n\}\n\nfunction buildActualPeriodCloseEntries/, `function buildExpectedPeriodCloseEntries(preview: PeriodClosePreviewResult) {\n  const entries: Array<{ accountCode: string; debit: number; credit: number }> = [];\n\n  for (const row of preview.incomeLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    if (row.amount > 0) {\n      entries.push({ accountCode: row.accountCode, debit: amount, credit: 0 });\n    } else {\n      entries.push({ accountCode: row.accountCode, debit: 0, credit: amount });\n    }\n  }\n\n  for (const row of preview.expenseLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    entries.push({ accountCode: row.accountCode, debit: 0, credit: round2(row.amount) });\n  }\n\n  const currentYearProfitEntry = buildCurrentYearProfitEntryFromPreview(preview);\n  if (hasMeaningfulAmount(currentYearProfitEntry.debit) || hasMeaningfulAmount(currentYearProfitEntry.credit)) {\n    entries.unshift({\n      accountCode: preview.currentYearProfitSubject.code,\n      debit: currentYearProfitEntry.debit,\n      credit: currentYearProfitEntry.credit,\n    });\n  }\n\n  if (preview.yearEndTransferRequired && preview.retainedEarningsSubject && hasMeaningfulAmount(preview.yearEndTransferAmount)) {\n    if (preview.yearEndCurrentYearProfitBalance > 0) {\n      entries.push({\n        accountCode: preview.currentYearProfitSubject.code,\n        debit: preview.yearEndTransferAmount,\n        credit: 0,\n      });\n      entries.push({\n        accountCode: preview.retainedEarningsSubject.code,\n        debit: 0,\n        credit: preview.yearEndTransferAmount,\n      });\n    } else {\n      entries.push({\n        accountCode: preview.retainedEarningsSubject.code,\n        debit: preview.yearEndTransferAmount,\n        credit: 0,\n      });\n      entries.push({\n        accountCode: preview.currentYearProfitSubject.code,\n        debit: 0,\n        credit: preview.yearEndTransferAmount,\n      });\n    }\n  }\n\n  return entries\n    .map((item) => ({\n      accountCode: String(item.accountCode || '').trim(),\n      debit: round2(Number(item.debit || 0)),\n      credit: round2(Number(item.credit || 0)),\n    }))\n    .filter((item) => item.accountCode && (hasMeaningfulAmount(item.debit) || hasMeaningfulAmount(item.credit)))\n    .sort((a, b) => {\n      const codeCompare = a.accountCode.localeCompare(b.accountCode);\n      if (codeCompare !== 0) return codeCompare;\n      if (a.debit !== b.debit) return a.debit - b.debit;\n      return a.credit - b.credit;\n    });\n}\n\nfunction buildActualPeriodCloseEntries`,);

api = replaceExact(
  api,
  `  const warnings = buildWarnings({ incomeLines, expenseLines, contributionMap });\n  const totalIncome = round2(incomeLines.reduce((sum, item) => sum + Math.max(item.amount, 0), 0));\n  const totalExpense = round2(expenseLines.reduce((sum, item) => sum + Math.max(item.amount, 0), 0));\n  const incomeLossAdjustment = round2(\n    incomeLines.filter((item) => item.amount < -EPSILON).reduce((sum, item) => sum + Math.abs(item.amount), 0),\n  );\n  const expenseProfitAdjustment = round2(\n    expenseLines.filter((item) => item.amount < -EPSILON).reduce((sum, item) => sum + Math.abs(item.amount), 0),\n  );\n  const profitAmount = round2(totalIncome - totalExpense - incomeLossAdjustment + expenseProfitAdjustment);\n\n  let existingVoucherReusable = false;\n`,
  `  const warnings = buildWarnings({ incomeLines, expenseLines, contributionMap });\n  const totalIncome = round2(incomeLines.reduce((sum, item) => sum + Math.max(item.amount, 0), 0));\n  const totalExpense = round2(expenseLines.reduce((sum, item) => sum + Math.max(item.amount, 0), 0));\n  const incomeLossAdjustment = round2(\n    incomeLines.filter((item) => item.amount < -EPSILON).reduce((sum, item) => sum + Math.abs(item.amount), 0),\n  );\n  const expenseProfitAdjustment = round2(\n    expenseLines.filter((item) => item.amount < -EPSILON).reduce((sum, item) => sum + Math.abs(item.amount), 0),\n  );\n  const profitAmount = round2(totalIncome - totalExpense - incomeLossAdjustment + expenseProfitAdjustment);\n\n  const currentYearProfitEntry = buildCurrentYearProfitEntryFromPreview({\n    period,\n    startDate,\n    endDate,\n    voucherDate,\n    incomeLines,\n    expenseLines,\n    totalIncome,\n    totalExpense,\n    profitAmount,\n    warnings,\n    existingVoucher,\n    existingVoucherReusable: false,\n    existingVoucherReuseMessage: '',\n    currentYearProfitSubject: dataset.currentYearProfitSubject,\n    retainedEarningsSubject: dataset.retainedEarningsSubject,\n    incomeTaxExpenseSubject: dataset.incomeTaxExpenseSubject,\n    yearEndTransferRequired: false,\n    yearEndCurrentYearProfitBalance: 0,\n    yearEndTransferAmount: 0,\n  });\n\n  const yearEndTransferBaseBalance = isYearEndPeriod(period)\n    ? await getCurrentYearProfitBalanceForYearEnd({\n        period,\n        endDate,\n        accountSetId: dataset.datasetParams.accountSetId,\n        companyName: dataset.datasetParams.companyName,\n        excludeVoucherId: String(existingVoucher?.rowid || ''),\n      })\n    : 0;\n  const yearEndCurrentYearProfitBalance = isYearEndPeriod(period)\n    ? round2(yearEndTransferBaseBalance + currentYearProfitEntry.balanceChange)\n    : 0;\n  const yearEndTransferAmount = round2(Math.abs(yearEndCurrentYearProfitBalance));\n  const yearEndTransferRequired = Boolean(\n    isYearEndPeriod(period) &&\n      dataset.retainedEarningsSubject &&\n      hasMeaningfulAmount(yearEndTransferAmount),\n  );\n\n  let existingVoucherReusable = false;\n`,
  'preview year-end calculation',
);

api = replaceExact(
  api,
  `          currentYearProfitSubject: dataset.currentYearProfitSubject,\n          retainedEarningsSubject: dataset.retainedEarningsSubject,\n        },\n        existingVoucherData.details || [],\n      );\n`,
  `          currentYearProfitSubject: dataset.currentYearProfitSubject,\n          retainedEarningsSubject: dataset.retainedEarningsSubject,\n          incomeTaxExpenseSubject: dataset.incomeTaxExpenseSubject,\n          yearEndTransferRequired,\n          yearEndCurrentYearProfitBalance,\n          yearEndTransferAmount,\n        },\n        existingVoucherData.details || [],\n      );\n`,
  'compare preview payload',
);

api = replaceExact(
  api,
  `    existingVoucherReusable,\n    existingVoucherReuseMessage,\n    currentYearProfitSubject: dataset.currentYearProfitSubject,\n    retainedEarningsSubject: dataset.retainedEarningsSubject,\n  };\n}\n`,
  `    existingVoucherReusable,\n    existingVoucherReuseMessage,\n    currentYearProfitSubject: dataset.currentYearProfitSubject,\n    retainedEarningsSubject: dataset.retainedEarningsSubject,\n    incomeTaxExpenseSubject: dataset.incomeTaxExpenseSubject,\n    yearEndTransferRequired,\n    yearEndCurrentYearProfitBalance,\n    yearEndTransferAmount,\n  };\n}\n`,
  'preview return',
);

api = replaceExact(
  api,
  `      subjectNumbers: [CURRENT_YEAR_PROFIT_CODE, RETAINED_EARNINGS_CODE],\n      accountSetId: resolvedAccountSetId,\n    }),\n`,
  `      subjectNumbers: [CURRENT_YEAR_PROFIT_CODE, RETAINED_EARNINGS_CODE, INCOME_TAX_EXPENSE_CODE],\n      accountSetId: resolvedAccountSetId,\n    }),\n`,
  'load dataset fixed subjects',
);

api = api.replace(/export async function createPeriodCloseVoucherByPreview\([\s\S]*?\n\}\n\nexport async function createPeriodCloseVoucher\(/, `export async function createPeriodCloseVoucherByPreview(\n  preview: PeriodClosePreviewResult,\n  params: PeriodCloseParams,\n) {\n  if (preview.existingVoucher) {\n    if (!preview.existingVoucherReusable) {\n      throw new Error(preview.existingVoucherReuseMessage || '检测到损益类凭证已变化，请先删除原结转损益凭证，再重新结账。');\n    }\n    return {\n      preview,\n      voucherId: String(preview.existingVoucher.rowid || ''),\n      voucherCode: String(preview.existingVoucher.voucher_code || ''),\n    };\n  }\n\n  if (preview.incomeLines.length === 0 && preview.expenseLines.length === 0 && !preview.yearEndTransferRequired) {\n    return {\n      preview,\n      voucherId: '',\n      voucherCode: '',\n    };\n  }\n\n  const detailList: ErpVoucherApi.VoucherDetail[] = [];\n\n  for (const row of preview.incomeLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    if (row.amount > 0) {\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: row.accountCode,\n        account_name: row.accountName,\n        abstract_content: '结转损益',\n        debit_amount: amount,\n        credit_amount: 0,\n      });\n    } else {\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: row.accountCode,\n        account_name: row.accountName,\n        abstract_content: '结转损益',\n        debit_amount: 0,\n        credit_amount: amount,\n      });\n    }\n  }\n\n  for (const row of preview.expenseLines) {\n    const amount = Math.abs(round2(row.amount));\n    if (!hasMeaningfulAmount(amount)) continue;\n    detailList.push({\n      rowid: generateUUID(),\n      account_code: row.accountCode,\n      account_name: row.accountName,\n      abstract_content: '结转损益',\n      debit_amount: 0,\n      credit_amount: round2(row.amount),\n    });\n  }\n\n  const currentYearProfitEntry = buildCurrentYearProfitEntryFromPreview(preview);\n  if (hasMeaningfulAmount(currentYearProfitEntry.debit) || hasMeaningfulAmount(currentYearProfitEntry.credit)) {\n    detailList.unshift({\n      rowid: generateUUID(),\n      account_code: preview.currentYearProfitSubject.code,\n      account_name: preview.currentYearProfitSubject.name,\n      abstract_content: '结转损益',\n      debit_amount: currentYearProfitEntry.debit,\n      credit_amount: currentYearProfitEntry.credit,\n    });\n  }\n\n  if (preview.yearEndTransferRequired && preview.retainedEarningsSubject && hasMeaningfulAmount(preview.yearEndTransferAmount)) {\n    if (preview.yearEndCurrentYearProfitBalance > 0) {\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: preview.currentYearProfitSubject.code,\n        account_name: preview.currentYearProfitSubject.name,\n        abstract_content: '年终结转本年利润',\n        debit_amount: preview.yearEndTransferAmount,\n        credit_amount: 0,\n      });\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: preview.retainedEarningsSubject.code,\n        account_name: preview.retainedEarningsSubject.name,\n        abstract_content: '年终结转本年利润',\n        debit_amount: 0,\n        credit_amount: preview.yearEndTransferAmount,\n      });\n    } else {\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: preview.retainedEarningsSubject.code,\n        account_name: preview.retainedEarningsSubject.name,\n        abstract_content: '年终结转本年利润',\n        debit_amount: preview.yearEndTransferAmount,\n        credit_amount: 0,\n      });\n      detailList.push({\n        rowid: generateUUID(),\n        account_code: preview.currentYearProfitSubject.code,\n        account_name: preview.currentYearProfitSubject.name,\n        abstract_content: '年终结转本年利润',\n        debit_amount: 0,\n        credit_amount: preview.yearEndTransferAmount,\n      });\n    }\n  }\n\n  const totalDebitAmount = round2(\n    detailList.reduce((sum, item) => sum + Number(item.debit_amount || 0), 0),\n  );\n  const totalCreditAmount = round2(\n    detailList.reduce((sum, item) => sum + Number(item.credit_amount || 0), 0),\n  );\n  const resolvedAccountSetId = resolveAccountSetId(params.accountSetId);\n  const nextVoucher = await getMonthlyNextVoucherCode({\n    voucherDate: preview.voucherDate,\n    accountSetId: resolvedAccountSetId,\n    companyName: params.companyName,\n    voucherWord: '记',\n  });\n\n  const res = await createVoucher(\n    {\n      rowid: generateUUID(),\n      voucher_type: nextVoucher.voucherWord,\n      voucher_code: nextVoucher.voucherCode,\n      business_name: preview.yearEndTransferRequired ? '结转损益（含本年利润转未分配利润）' : '结转损益',\n      business_code: buildPeriodBusinessCode(params.period),\n      description: JSON.stringify({\n        type: 'period-close',\n        period: params.period,\n        startDate: preview.startDate,\n        endDate: preview.endDate,\n        totalIncome: preview.totalIncome,\n        totalExpense: preview.totalExpense,\n        profitAmount: preview.profitAmount,\n        warnings: preview.warnings,\n        voucherWord: nextVoucher.voucherWord,\n        voucherNo: nextVoucher.voucherNo,\n        yearEndTransferRequired: preview.yearEndTransferRequired,\n        yearEndCurrentYearProfitBalance: preview.yearEndCurrentYearProfitBalance,\n        yearEndTransferAmount: preview.yearEndTransferAmount,\n        retainedEarningsSubject: preview.retainedEarningsSubject,\n      }),\n      voucher_date: preview.voucherDate,\n      operator: params.operator,\n      reviewer: params.reviewer,\n      account_set_id: resolvedAccountSetId || undefined,\n      debit_amount: totalDebitAmount,\n      credit_amount: totalCreditAmount,\n      is_posted: 0,\n      is_reversed: 0,\n    },\n    detailList.map((item) => ({\n      ...item,\n      account_set_id: resolvedAccountSetId || item.account_set_id,\n    })),\n  );\n\n  return {\n    preview,\n    voucherId: String((res as any)?.rowid || ''),\n    voucherCode: String((res as any)?.voucher_code || nextVoucher.voucherCode || ''),\n  };\n}\n\nexport async function createPeriodCloseVoucher(`);

fs.writeFileSync(apiFile, api, 'utf8');

let view = fs.readFileSync(viewFile, 'utf8');
view = replaceExact(
  view,
  `function buildClosePreviewTagText() {\n  const incomeCount = closePreview.value?.incomeLines?.length ?? 0;\n  const expenseCount = closePreview.value?.expenseLines?.length ?? 0;\n  if (incomeCount === 0 && expenseCount === 0) {\n    return '本期无损益数据，可直接结账';\n  }\n  return \`收入 \${incomeCount} 条 / 费用 \${expenseCount} 条\`;\n}\n`,
  `function buildClosePreviewTagText() {\n  const incomeCount = closePreview.value?.incomeLines?.length ?? 0;\n  const expenseCount = closePreview.value?.expenseLines?.length ?? 0;\n  if (incomeCount === 0 && expenseCount === 0) {\n    return closePreview.value?.yearEndTransferRequired\n      ? '本期无损益数据，年终仍需结转本年利润'\n      : '本期无损益数据，可直接结账';\n  }\n  return \`收入 \${incomeCount} 条 / 费用 \${expenseCount} 条\`;\n}\n`,
  'view close tag',
);
view = replaceExact(
  view,
  `const currentStep = computed(() => {\n  if (activeTab.value === 'reverse') return reversePreview.value?.canReverse ? 2 : 1;\n  return finishVoucherId.value || closePreview.value?.existingVoucher ? 2 : 1;\n});\n`,
  `const currentStep = computed(() => {\n  if (activeTab.value === 'reverse') return reversePreview.value?.canReverse ? 2 : 1;\n  const finalStep = closePreview.value?.yearEndTransferRequired ? 3 : 2;\n  return finishVoucherId.value || closePreview.value?.existingVoucher ? finalStep : 1;\n});\n`,
  'view current step',
);
view = replaceExact(
  view,
  `              点击某个月份时，系统会直接按该月期间范围和当前卡片所在账套查询全部凭证与明细，现场计算期间结账预览；执行时继续沿用该账套与公司信息生成期间结转凭证并回写期间状态。页面会按每个账套的启用月份连续展示期间，并至少显示到当前月份；若最新已结账月份更靠后，则继续展示到下一月份。空月份若没有损益数据，也允许直接结账推进到下个月。\n`,
  `              点击某个月份时，系统会直接按该月期间范围和当前卡片所在账套查询全部凭证与明细，现场计算期间结账预览；执行时继续沿用该账套与公司信息生成期间结转凭证并回写期间状态。普通月份按收入、费用和所得税费用结转到本年利润；12 月在结转损益后，还会把本年利润余额结转到未分配利润。页面会按每个账套的启用月份连续展示期间，并至少显示到当前月份；若最新已结账月份更靠后，则继续展示到下一月份。空月份若没有损益数据，也允许直接结账推进到下个月。\n`,
  'view page desc',
);
view = replaceExact(
  view,
  `            <el-steps :active="currentStep" finish-status="success" align-center>\n              <el-step title="结转损益准备" description="核对本月损益结转金额" />\n              <el-step title="结转损益" description="生成/复用结转损益凭证" />\n              <el-step title="完成结账" description="回写期间状态为已结账" />\n            </el-steps>\n`,
  `            <el-steps :active="currentStep" finish-status="success" align-center>\n              <el-step title="结转损益准备" description="核对本月损益结转金额" />\n              <el-step title="结转损益" description="生成/复用结转损益凭证" />\n              <el-step\n                v-if="closePreview.yearEndTransferRequired"\n                title="年终结转"\n                description="将本年利润余额转入未分配利润"\n              />\n              <el-step title="完成结账" description="回写期间状态为已结账" />\n            </el-steps>\n`,
  'view steps',
);
view = replaceExact(
  view,
  `                <el-tag>{{ \`利润 \${formatMoney(closePreview.profitAmount)}\` }}</el-tag>\n`,
  `                <el-tag>{{ \`利润 \${formatMoney(closePreview.profitAmount)}\` }}</el-tag>\n                <el-tag v-if="closePreview.incomeTaxExpenseSubject" type="warning">\n                  {{ \`含所得税科目 \${closePreview.incomeTaxExpenseSubject.code}\` }}\n                </el-tag>\n                <el-tag v-if="closePreview.yearEndTransferRequired" type="success">\n                  {{ \`年终结转 \${formatMoney(closePreview.yearEndTransferAmount)}\` }}\n                </el-tag>\n`,
  'view tags',
);
view = replaceExact(
  view,
  `              <div class="panel-hint">\n                该步骤会读取当前月份已录入的凭证，汇总本期损益类科目后结转到本年利润；若本期没有损益数据，则不生成结转损益凭证，直接完成关账；若原结转损益凭证与当前损益数据一致，则直接复用并结账；若已不一致，需先删除旧结转损益凭证，再重新结账。\n              </div>\n`,
  `              <div class="panel-hint">\n                该步骤会读取当前月份已录入的凭证，汇总本期损益类科目后结转到本年利润，并额外兜底纳入所得税费用；若本期没有损益数据，则不生成结转损益凭证，直接完成关账；若为 12 月，系统还会在结转损益后将本年利润余额转入未分配利润；若原结转损益凭证与当前损益数据一致，则直接复用并结账；若已不一致，需先删除旧结转损益凭证，再重新结账。\n              </div>\n`,
  'view panel hint',
);
view = replaceExact(
  view,
  `                <div class="close-preview-box">\n                  <div class="preview-label">已有结转损益凭证</div>\n                  <div class="preview-value small">{{ buildExistingVoucherText() }}</div>\n                </div>\n`,
  `                <div class="close-preview-box">\n                  <div class="preview-label">已有结转损益凭证</div>\n                  <div class="preview-value small">{{ buildExistingVoucherText() }}</div>\n                </div>\n                <div v-if="closePreview.yearEndTransferRequired" class="close-preview-box">\n                  <div class="preview-label">年终本年利润余额</div>\n                  <div class="preview-value">{{ formatMoney(closePreview.yearEndCurrentYearProfitBalance) }}</div>\n                </div>\n                <div v-if="closePreview.yearEndTransferRequired" class="close-preview-box">\n                  <div class="preview-label">转入未分配利润</div>\n                  <div class="preview-value">{{ formatMoney(closePreview.yearEndTransferAmount) }}</div>\n                </div>\n`,
  'view year-end boxes',
);
view = replaceExact(
  view,
  `              执行结转损益并结账\n`,
  `              {{ closePreview?.yearEndTransferRequired ? '执行结转损益、年终结转并结账' : '执行结转损益并结账' }}\n`,
  'view button text',
);
fs.writeFileSync(viewFile, view, 'utf8');

const skill = `# 期末结转 / 反结账页面 skill\n\n## 页面入口\n- 路径：\`lmbill/apps/web-ele/src/views/erp/finance/period/index.vue\`\n- 页面名称：期末处理 / 反结账\n\n## 页面能力\n- 按账套、公司、年度展示会计期间卡片。\n- 支持期末结转、结账、反结账。\n- 支持按期间查看结转损益预览与反结账预览。\n- 12 月结账时，在结转损益后自动将本年利润余额转入未分配利润。\n- 页面预览会显示所得税费用兜底识别结果与年终结转金额。\n\n## 状态规则\n- 绿色：\`未结账\`\n- 蓝色：\`已结转损益，未结账\`\n- 灰色：\`已结账\`\n\n## 本次修改\n- 结转损益时对\`所得税费用\`做了兜底识别，避免科目分类不标准时漏结转到\`3103 本年利润\`。\n- 12 月结账新增“年终结转”步骤：将\`3103 本年利润\`余额自动转到\`3104006 未分配利润\`。\n- 结账预览增加年终本年利润余额、转入未分配利润金额、所得税科目提示。\n- 结账按钮文案在 12 月自动切换为“执行结转损益、年终结转并结账”。\n- 生成凭证时，年终月份会把“结转损益 + 本年利润转未分配利润”写入同一张期间结转凭证。\n\n## 使用到的数据 / 接口\n- \`getPeriodStatusList\`：查询期间状态列表\n- \`savePeriodStatus\`：保存期间状态\n- \`getPeriodClosePreview\`：获取结账预览\n- \`createPeriodCloseVoucherByPreview\`：生成结转损益/年终结转凭证并完成结账\n- \`getPeriodReversePreview\`：获取反结账预览\n- \`reversePeriodClose\`：执行反结账\n- \`getAccountCurrentAccount\`：查询当前账套\n- 底层数据表：\`Bil_Subject_Info\`、\`Bil_Voucher_Main\`、\`Bil_Voucher_Detail\`\n\n## 关键状态字段\n- \`carry_forward_status\`\n- \`close_status\`\n- \`period_code\`\n- \`account_set_id\`\n- \`company_name\`\n`;
fs.writeFileSync(skillFile, skill, 'utf8');

console.log('patched');
